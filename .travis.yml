language: node_js
node_js:
  -"node"
  -"6"
  -"5"
addons:
  chrome: stable

env:
  -NODE_ENV=test

before_script:
  -chmod 0777 ./node_modules/.bin/mocha
  -curl -L https://codeclimate.com/downloads/test-reporter/test-reporter-latest-linux-amd64 > ./cc-test-reporter
  -chmod +x ./cc-test-reporter
  -./cc-test-reporter before-build
 -psql -c "CREATE DATABASE bookmeal;" -U postgres

 -npm install codeclimate-test-reporter istanbul -g

 CODECLIMATE_REPO_TOKEN: f05e52daa7be717108dbd8228ce88f00a8bb39946b312c6dd07641abac0aa8e8

services:
- postgresql

after_script:

- codeclimate-test-reporter < ./coverage/lcov.info

script:
 -npm run test:server

after_success:
  -npm run coveralls
  -./cc-test-reporter after-build --exit-code $TRAVIS_TEST_RESULT
  
deploy:
  provider: heroku
  api_key:
    secure:
  provider: pages
  skip-cleanup: true
  github-token: WRNsa5WnR1-GY979OZsdAg
  keep-history: true
  on:
    branch: develop


    